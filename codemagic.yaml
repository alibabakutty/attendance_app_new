workflows:
  ios-release:
    name: iOS Release

    environment:
      flutter: stable
      xcode: latest

    integrations:
      app_store_connect: Kanishkaa attendance app

    scripts:
      - flutter pub get
      - flutter clean
      - flutter build ipa --release

    artifacts:
      - build/ios/ipa/*.ipa